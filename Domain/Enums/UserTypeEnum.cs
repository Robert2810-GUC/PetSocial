namespace Domain.Enums;

public enum UserTypeEnum
{
        // User Subtypes (start from 100 range to avoid overlap)
    PetOwner = 1,
    PetBusiness = 2,
    ContentCreator = 3,


    // Main Types
    Admin = 101,
    User = 102
    
}

